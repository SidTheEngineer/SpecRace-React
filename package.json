{
  "name": "specrace-express-server",
  "version": "0.0.1",
  "description": "This is the express server that will server the SpecRace client-side app.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node build/server.js",
    "install": "cd client && npm install && npm run build && cd ..",
    "start": "npm run build && nf start -p 3000",
    "build": "babel server.js -d build"
  },
  "author": "Sid P.",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.14.0",
    "express": "^4.14.0",
    "foreman": "^2.0.0",
    "memory-cache": "^0.1.6"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-latest": "^6.16.0",
    "json-loader": "^0.5.4"
  }
}
