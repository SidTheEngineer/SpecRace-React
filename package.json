{
  "name": "specrace-express-server",
  "version": "0.0.1",
  "description": "This is the express server that will server the SpecRace client-side app.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node server.js",
    "start": "nf start -p 3000"
  },
  "author": "Sid P.",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "foreman": "^2.0.0"
  },
  "proxy": "http://localhost:3001/"
}
